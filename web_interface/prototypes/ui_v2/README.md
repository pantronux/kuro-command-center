# UI V2 Static Reference

This is a dependency-free visual reference translated from
`Kuro-UI-Prototype-main/artifacts/kuro-ai`.

It is not production, does not call backend APIs, and must not replace the V1
Jinja dashboard.

Open `index_static.html` directly in a browser to inspect the mock states.

